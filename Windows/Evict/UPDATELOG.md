# updatelog

## Pre-release
- Initial tool: migrate common dev-tool directories off the system drive to a user-chosen location, using environment variables / config files only (no symlinks), terminal UI
